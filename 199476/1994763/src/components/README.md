# Components

- `layout/`: reusable page shells and structural components.
- `navigation/`: global navigation components.

Business pages are composed by the application layer. New reusable UI belongs here rather than being duplicated in pages.
